import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'serenity-timer-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
